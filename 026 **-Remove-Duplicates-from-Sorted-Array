public class Solution {
    public int removeDuplicates(int[] nums) {
        if(nums == null)
            return 0;

        int j = 1;
        for(int i=1; i<nums.length; i++){
            if(nums[i]==nums[i-1]){
                continue;
            }else
            {
                 nums[j] = nums[i]; 
                 j++;
             }
        }

       return j;
    }
}
